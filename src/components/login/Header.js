import React from 'react';
import {Typography} from '@material-ui/core';


const Header = () => {
  return (
    <div>
      <Typography variant="h5">Welcome to the (Virtual) Stillwater Pub Quiz!</Typography>
    </div>
  );
};

export default Header;